interface:
  display_name: T6 수신 배포
  short_description: receiver·내부 HTTPS·런타임 키 주입을 구성한다.
  default_prompt: $t6-receiver-deployment 스킬을 사용해 현재 저장소와 승인 범위를 먼저 확인하고 해당 절차를 수행해줘. 미승인 변경과 실제 AWS 적용은 하지 마.
policy:
  allow_implicit_invocation: false